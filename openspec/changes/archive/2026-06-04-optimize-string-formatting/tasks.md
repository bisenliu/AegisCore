## 1. 审查与定位

- [x] 1.1 检查 `user-services/internal/domain/user_status.go` 中 `UserStatus.AllowedValues` 的 `fmt.Sprint` 用法，确认当前输出值和调用语义。
- [x] 1.2 在 `common/` 和 `user-services/` 范围内搜索 `fmt.Sprint`、`fmt.Sprintf`、`fmt.Sprintln` 等字符串格式化用法，排除生成代码和第三方代码。
- [x] 1.3 逐项分类搜索结果：简单类型转换、简单字符串拼接、错误/日志/复合模板格式化、格式控制或调试输出。
- [x] 1.4 检查 `user-services/internal/domain/user_status.go` 中 `UserStatus` 常量定义缺少状态含义注释的问题，确认 `100` 正常、`200` 冻结/停用、`300` 必须修改密码的业务含义和兼容性边界。
- [x] 1.5 在项目范围内审查类似问题：承载业务状态、认证协议、Redis key 协议、安全参数、响应契约、校验契约或环境变量开关的常量定义缺少必要注释，排除生成代码和测试 fixture。
- [x] 1.6 按收益分类常量注释候选：优先处理 `UserStatus`、JWT subject、Redis session key/TTL、密码哈希参数等高价值候选；对名称和值已自解释、已有组注释或补注释会重复标准定义的常量保持不变并记录依据。

## 2. 实现优化

- [x] 2.1 将 `UserStatus.AllowedValues` 的固定枚举允许值改为字符串字面量或等价直接实现，并移除不再需要的 `fmt` import。
- [x] 2.2 对项目范围内确认属于不必要简单转换或拼接的格式化用法做最小替换，保持输出完全一致。
- [x] 2.3 保留无明显收益或替换后可读性下降的格式化用法，并记录每类保留依据用于最终说明。
- [x] 2.4 为 `UserStatus` 常量补充业务含义注释，说明各状态码含义、登录影响以及数值作为持久化/API 契约时的兼容性要求。
- [x] 2.5 为项目范围内确认高价值的类似常量补充简洁注释，优先解释业务/协议/安全边界，而不是重复常量名和值。
- [x] 2.6 对低收益常量注释场景保持不变，例如已有高质量注释、标准 header 名、默认分页值、测试 fixture 或常量名和值已充分自解释的场景，并记录最终判断依据。
- [x] 2.7 确认不涉及 Ent schema、生成代码、数据库 migration、HTTP API、配置或错误码变更，因此无需运行 `go generate ./ent` 或 Atlas migration 流程。

## 3. 验证与说明

- [x] 3.1 对修改过的 Go 文件运行 `gofmt`。
- [x] 3.2 运行受影响模块测试，优先执行 `go test ./...` 于 `user-services/`，如涉及 `common/` 修改则同时在 `common/` 执行。
- [x] 3.3 复核 `AllowedValues` 输出、枚举校验和 JSON/text 反序列化行为未改变。
- [x] 3.4 复核新增注释仅解释既有业务/协议含义，不改变常量值、函数行为、API 响应、错误码、配置 key 或数据库 schema。
- [x] 3.5 在最终结果中列出每处格式化修改的原因和优化方式、每处常量注释补充的原因，以及未修改格式化/注释候选场景的判断依据和验证结果。
